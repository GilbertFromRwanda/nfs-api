# nfs-api

## Regenerating docs

After making changes to annotations, run:

swag init --parseDependency --parseInternal

### Swagger UI access

Once the server is running, visit <http://localhost:8080/swagger/index.html>
