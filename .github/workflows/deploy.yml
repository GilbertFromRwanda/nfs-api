name: Build & Deploy

on:
  push:
    branches: [main]

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.21'

      - name: Download dependencies
        run: go mod download

      - name: Build
        run: CGO_ENABLED=0 go build -ldflags="-s -w" -o app-bin .

  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    needs: build
    steps:
      - uses: actions/checkout@v4

      - name: Install Andasy CLI
        run: curl -fsSL https://get.andasy.dev | sh

      - name: Deploy to Andasy
        run: andasy deploy
        env:
          ANDASY_TOKEN: ${{ secrets.ANDASY_TOKEN }}
